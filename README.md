# Videojuegos App

Aplicación en React para listar videojuegos, filtrar por categorías y plataformas, buscar, ver detalles y eliminar.

## Scripts

- `npm run dev` → Levanta la app React en `http://localhost:3000`
- `npm run server` → Levanta json-server en `http://localhost:5000`
